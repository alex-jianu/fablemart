<!DOCTYPE html>
<html lang="en">

<head>
  <title>Acebook - Homepage</title>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</head>

<body>
  <header>
    <span>
      <form action="/sessions?_method=DELETE" method="POST">
        <a href="/items">FableMart</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <span>
          <input type="text" placeholder="Search..">
        </span>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/items/new">New Item</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/messages">Messages</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/orders">Orders</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <input type="submit" value="Log Out">
      </form>
    </span>
  </header>
  <span>
    <a href="/items/a-z">A-Z</a>
    <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
    <a href="/items/most-popular">Most Praised</a>
    <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
    <a href="/items/most-recent">Most Recent</a>
    <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
    <a href="/items/most-viewed">Most viewed</a>
    <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
    <a href="/items/liked-by-you">Liked by You</a>
  </span>

  <div>
    <% items.forEach(function (item) { %>
    <div>
      <img src="<%= item.photo %>" height="100" width="100">
      <br>
      <a href="/items/<%= item.id%>" <p>Name: <%= item.name %></p></a>
      <p><%= item.description %></p>
      <p>Posted by: <%= item.owner %></p>

      <% if (item.likedBy.length === 0) { %>
      <p>No Likes</p>
      <% } else if (item.likedBy.length === 1) { %>
      <p><%= item.likedBy.length %> Like</p>
      <% } else { %>
      <p><%= item.likedBy.length %> Likes</p>
      <% } %>

      <% if (item.viewedBy.length === 0) { %>
      <p>No Views</p>
      <% } else if (item.viewedBy.length === 1) { %>
      <p><%= item.viewedBy.length %> View</p>
      <% } else { %>
      <p><%= item.viewedBy.length %> Views</p>
      <% } %>

      <br>
      <hr>
    </div>
    <% }) %>
  </div>

</body>
<footer>FableMart™ in partnership with Eta™</footer>

</html>