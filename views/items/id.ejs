<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <title>FableMart™ <%= item.name%></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <link rel='stylesheet' href='/stylesheets/items-id.css' />
</head>

<body>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  <header>

    <nav>
      <span class="container-fluid justify-content-center">
        <form action="/sessions?_method=DELETE" method="POST"></form>
        <a href="/items"><img src="/images/fablemart-logo-without-decoration.png" alt="fablemart" height="50px" width="100px"></a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <span>
          <input type="text" placeholder="Search..">
        </span>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/items/new">New Item</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/user/:username">Profile</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/messages">Messages</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/orders">Orders</a>
        <span class="larger">&nbsp;&nbsp;|&nbsp;&nbsp;</span>
        <a href="/sessions/new">Log out</a>
        </form>
      </span>
    </nav>

  </header>
  <div class="content">
    <div class="photo">
      <img src="<%= item.photo %>" height="400" width="400">
    </div>
    <div class="lower-text">
      <div class="details">
        <p><%= item.name%></p>
        <br>
        <p><%= item.description%></p>
        <br>
        <a href="/users/<%=item.owner%>">
          <p>Owner: <%= item.owner%></p>
        </a>
        <br>
      </div>
      <div class="bottom-items">
        <div class="likes-views">
          <% if (item.likedBy.length === 0) { %>
          <p>No Likes</p>
          <% } else if (item.likedBy.length === 1) { %>
          <p><%= item.likedBy.length %> Like</p>
          <% } else { %>
          <p><%= item.likedBy.length %> Likes</p>
          <% } %>

          <% if (item.viewedBy.length === 0) { %>
          <p>No Views</p>
          <% } else if (item.viewedBy.length === 1) { %>
          <p><%= item.viewedBy.length %> View</p>
          <% } else { %>
          <p><%= item.viewedBy.length %> Views</p>
          <% } %>
        </div>

        <br>

        <div class="like-order">
          <form action="/items/<%= item.id%>/like?_method=PATCH" method="POST">
            <% if (item.isLiked) { %>
            <button><input class="logout" type="submit" value="Unlike"></button>
            <% } else { %>
            <button><input class="logout" type="submit" value="Like"></button>
            <% } %>
          </form>

          <form action="/orders/<%=item.id%>" method="POST">
            <button>
              <input class="logout" type="submit" value="Order">
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <nav style="position: fixed; bottom: 0%; right: 0%; left: 0%; padding-top:1%">
    <div class="justify-content-center">
      <p>FableMart™ Bringing imagination to life</p>
    </div>
  </nav>

</body>

</html>